let nome = 'leo' // (LET) Pode ser alterado - string  
nome = 'matheus'

var idade = 20 // (VAR) Pode ser alterado - number
idade = 19 

const maior_idade = true // (CONST) Não pode ser alterado - boolean  


// Array
const paises = ['brasil', 'argentina', 'chile']

// Objeto
const celular = {
    nome: 'iphone',
    valor: 8000
}

// Função
function soma(a, b) {
    return a + b
    
}